# 前端环境变量配置示例

# API 基础地址（可选）
# 开发环境：连接到你的 Workers
# 生产环境：如果前后端分离部署，需要配置完整的 Workers 地址
# VITE_API_BASE=https://api.domielabbott.workers.dev

# 是否使用 Mock API（仅开发环境有效）
# 设置为 "false" 可以在开发环境使用真实 API
# 生产环境始终使用真实 API
VITE_USE_MOCK=true
